# Anthology Atlas — build dependencies
# Install into your venv:
#     python -m venv venv
#     source venv/bin/activate
#     pip install -r requirements.txt
#
# These are only needed to BUILD the graph (build_graph_bulk.py / build_graph.py).
# The app itself (acl-litmap-full.html) is static and needs no Python.
# Python 3.11+ is required (the acl-anthology package needs it).

acl-anthology-py>=0.5.0    # ACL Anthology metadata (node set, titles, DOIs)
requests>=2.31.0           # HTTP calls to the Semantic Scholar API
ijson>=3.2.0               # streaming JSON parse of the large bulk snapshots
tqdm>=4.66.0               # progress bars during download / build
